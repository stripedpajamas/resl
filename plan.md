slack -> APIG -> edge lambda (go) --- invoke LangLamb with code inside event --->

LL is a container lambda
the container has all supported runtimes (go, node)
LL pipes code into a file, spawns, collects, returns


